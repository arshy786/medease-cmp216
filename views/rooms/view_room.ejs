<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>🏨 Room Details – MedEase</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<!-- Styles & Scripts -->
<link rel="stylesheet" href="/css/style.css">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
<script defer src="/js/darkmode.js"></script>
</head>
<body>
<div class="container">
<!-- Header -->
<header>
<h1><i class="fas fa-bed"></i> Room Details</h1>
<p>Here you can view the full details of this hospital room in the MedEase system.</p>
<%- include('../partials/darkmode_toggle.ejs') %>
</header>
<!-- Room Info Card -->
<main>
<section class="card detail-card" style="max-width: 600px; margin: 30px auto;">
<p><strong><i class="fas fa-door-closed"></i> Room Number:</strong> <%= room.roomNumber %></p>
<p><strong><i class="fas fa-tag"></i> Type:</strong> <%= room.type %></p>
<p><strong><i class="fas fa-user-check"></i> Occupied:</strong> <%= room.isOccupied ? 'Yes' : 'No' %></p>
<p><strong><i class="fas fa-calendar-alt"></i> Created On:</strong> <%= room.createdAt.toDateString() %></p>
<% if (room.assignedPatient) { %>
<p><strong><i class="fas fa-user"></i> Assigned Patient:</strong> <%= room.assignedPatient.name || 'N/A' %></p>
<% } %>
</section>
<!-- Action Buttons -->
<div class="button-group" style="text-align: center; margin-top: 20px;">
<a href="/rooms/<%= room._id %>/edit" class="btn edit" aria-label="Edit room">
<i class="fas fa-edit"></i> Edit Room
</a>
<form action="/rooms/<%= room._id %>?_method=DELETE" method="POST" style="display:inline;">
<button type="submit" class="btn delete" onclick="return confirm('Are you sure you want to delete this room?');" aria-label="Delete room">
<i class="fas fa-trash-alt"></i> Delete Room
</button>
</form>
<a href="/rooms" class="btn back" aria-label="Go back to all rooms">
<i class="fas fa-arrow-left"></i> Back to Rooms
</a>
<a href="/" class="btn home" aria-label="Return to home page">
<i class="fas fa-home"></i> Back to Home
</a>
</div>
</main>
<!-- Footer -->
<footer>
<p>&copy; 2025 Arshad Aslam – CMP216 Web Applications and Cloud Services</p>
</footer>
</div>
</body>
</html>