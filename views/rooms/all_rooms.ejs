<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>🏥 All Rooms – MedEase</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<!-- Styles and Scripts -->
<link rel="stylesheet" href="/css/style.css">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
<script defer src="/js/darkmode.js"></script>
</head>
<body>
<div class="container">
<!-- Header Section -->
<header>
<h1><i class="fas fa-door-closed"></i> All Hospital Rooms</h1>
<p>Browse and manage all registered rooms within the MedEase system.</p>
<%- include('../partials/darkmode_toggle.ejs') %>
</header>
<!-- Navigation Buttons -->
<section class="button-group" style="text-align: center; margin: 20px 0;">
<a href="/rooms/new" class="btn add" aria-label="Add a new room">
<i class="fas fa-plus-circle"></i> Add New Room
</a>
<a href="/" class="btn home" aria-label="Return to home page">
<i class="fas fa-home"></i> Back to Home
</a>
</section>
<!-- Rooms Table or Message -->
<% if (rooms.length === 0) { %>
<p class="notice" role="alert" style="text-align: center;">⚠️ No rooms found. Please add a new room to get started.</p>
<% } else { %>
<table class="table" aria-label="List of all hospital rooms">
<thead>
<tr>
<th><i class="fas fa-hashtag"></i> Room Number</th>
<th><i class="fas fa-tag"></i> Type</th>
<th><i class="fas fa-user-check"></i> Occupied</th>
<th><i class="fas fa-cogs"></i> Actions</th>
</tr>
</thead>
<tbody>
<% rooms.forEach(room => { %>
<tr>
<td><%= room.roomNumber %></td>
<td><%= room.type %></td>
<td><%= room.isOccupied ? '✅ Yes' : '❌ No' %></td>
<td>
<a href="/rooms/<%= room._id %>" class="btn view" aria-label="View room <%= room.roomNumber %>">
<i class="fas fa-eye"></i> View
</a>
<a href="/rooms/<%= room._id %>/edit" class="btn edit" aria-label="Edit room <%= room.roomNumber %>">
<i class="fas fa-edit"></i> Edit
</a>
<form action="/rooms/<%= room._id %>?_method=DELETE" method="POST" style="display: inline;">
<button type="submit" class="btn delete" onclick="return confirm('Are you sure you want to delete this room?');" aria-label="Delete room <%= room.roomNumber %>">
<i class="fas fa-trash-alt"></i> Delete
</button>
</form>
</td>
</tr>
<% }) %>
</tbody>
</table>
<% } %>
<!-- Footer -->
<footer>
<p>&copy; 2025 Arshad Aslam – CMP216 Web Applications and Cloud Services</p>
</footer>
</div>
</body>
</html>